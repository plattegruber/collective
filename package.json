{
  "name": "gg-workers",
  "private": true,
  "scripts": {
    "build": "esbuild workers/src/worker.ts --bundle --format=esm --outfile=workers/dist/worker.mjs",
    "deploy": "wrangler publish"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "wrangler": "^3.72.0",
    "typescript": "^5.5.0"
  }
}
